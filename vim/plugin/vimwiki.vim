" prevent global wikis which causes all markdown files to be treated as vimwiki
" files
let g:vimwiki_global_ext = 0

" use markdown for vimwiki
let g:vimwiki_list = [{'path': '~/Dropbox/vimwiki/', 'syntax': 'markdown', 'ext': '.md'}]
